#using an example to create a list range using even numbers, starting with 2 and ending at 11 (10)
even_numbers = list(range(2,11,2))
print(even_numbers)

#in this example, the range starts with value 2, goes into 11 but adds 2 repeatedly until it hits 11. see print

