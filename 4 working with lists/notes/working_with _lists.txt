chapter 4 is working with lists more indepth, such as looping.
going over loops;
magicians.py

//examples of incorrect indents;//
hello_world.py 

//making lists of numbers using range();//
even_numbers.py continues this
square_numbers.py continues this, plus with list comprehensions (writing the same code in one line*)

//simple statistics with list examples;//
players.py

//slicing a list;//
players.py

//copying a list;//
foods.py

*more advanced ways of writing lists, included because when looking at others code this will be used often from the book. 

//tuples//
creating a list that cannot change. python refers to values that cannot change as immutable, and an immutable list is called a tuple
dimensions.py